/*
import React from "react";

export class FoundGameButton extends React.Component {
    getFullGame = () => {
        this.props.getFullGame(this.props.gameName);
    };

    render() {
        if (this.props.hidden) {
            return null;
        }
        else {
            return (
                <button className = "foundGame" onClick={this.getFullGame}>
                    {this.props.gameName}
                </button>
            )
        }
    };
}*/